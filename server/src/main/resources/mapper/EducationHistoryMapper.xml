<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lin.server.mapper.EducationHistoryMapper">

    <resultMap id="EducationHistoryResultMap" type="com.lin.pojo.entity.EducationHistory">
        <id property="id" column="id"/>
        <result property="resumeId" column="resume_id"/>
        <result property="school" column="school"/>
        <result property="major" column="major"/>
        <result property="degree" column="degree"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <select id="selectByResumeId" resultMap="EducationHistoryResultMap">
        SELECT * FROM education_history
        WHERE resume_id = #{resumeId} AND is_delete = 0
        ORDER BY start_date DESC
    </select>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO education_history (resume_id, school, major, degree, start_date, end_date)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.resumeId}, #{item.school}, #{item.major}, #{item.degree},
             #{item.startDate}, #{item.endDate})
        </foreach>
    </insert>

    <update id="deleteByResumeId">
        UPDATE education_history
        SET is_delete = 1
        WHERE resume_id = #{resumeId} AND is_delete = 0
    </update>

</mapper>
